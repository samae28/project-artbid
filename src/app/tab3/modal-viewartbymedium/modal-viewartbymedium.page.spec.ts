import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModalViewartbymediumPage } from './modal-viewartbymedium.page';

describe('ModalViewartbymediumPage', () => {
  let component: ModalViewartbymediumPage;
  let fixture: ComponentFixture<ModalViewartbymediumPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ModalViewartbymediumPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
